#include<bits/stdc++.h>
using namespace std;
int main()
{
    int count=0;
     long long int n,i;
    cin>>n;
    for( i=2;i<sqrt(n);i++)
    {
        if(n%i==0)
        {
           count++;
            break;
        }
    }
    if(count==0)
    {
        cout<<"YES"<<endl;
    }
    else
        {
          cout<<"NO";
        }

    return 0;
    }
